meta {
  name: List Active Conversations
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/api/chat/conversations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
